[package]
name = "showdown_valid"
type = "bin"
authors = ["Stellar Poker"]
compiler_version = ">=0.36.0"

[dependencies]
stellar_poker_lib = { path = "../lib" }

[package.metadata.circuit]
description = "Derive same shared deck, verify deck root, verify player hand commitments, evaluate all 7-card hands, output winner index."
constraint_count = 237018
proof_size_bytes = 16256
public_inputs_bytes = 832
poseidon2_permutations = 64
max_players = 6
